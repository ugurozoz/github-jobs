import * as ActionType from '../action-types/';

const initialState = {
  description: '',
  location: '',
  lat: null,
  long: null,
  full: false,
};


const filterReducer = (state = initialState, action) => { 


}
